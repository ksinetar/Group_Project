<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kevin.group_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Search" />
        <activity android:name=".Chat" />
        <activity android:name=".SearchGroupInfo" />
        <activity android:name=".ChatSettingsAdmin" />
        <activity android:name=".ChatSettingsMember" />
        <activity android:name=".ChatMemberProfile" />
        <activity android:name=".SignUp2" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".CreateGroup" />
        <activity android:name=".Profile"></activity>
    </application>

</manifest>